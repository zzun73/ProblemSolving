package leetcode.easy.Num1587;

class Solution {
/*
    # Write your MySQL query statement below
    SELECT U.name,sum(T.amount) as 'balance'
    FROM Users U JOIN Transactions T ON U.account=T.account
    GROUP BY T.account
    HAVING balance > 10000;
 */
}
