package leetcode.easy.Num1667;

class Solution {
/*
    # Write your MySQL query statement below
    SELECT user_id, concat(upper(substr(name,1,1)),lower(substr(name,2,length(name)))) AS name
    FROM Users
    ORDER BY user_id;
 */
}
